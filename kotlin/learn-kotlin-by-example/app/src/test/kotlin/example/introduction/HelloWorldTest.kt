/* Kotlin code is usually defined in packages. Package specification is optional
If you don't specify a package in a source file, its content goes to the default package */
package example.introduction

/*
An entry point to a Kotlin application is the main function.
In Kotlin 1.3, you can declare main without any parameters.

In Kotlin versions earlier than 1.3, the main function must have a parameter of type Array<String>.

fun main(args: Array<String>) {
    println("Hello, World!")
}
*/
fun main() {
    println("Hello, World!!")
    //The return type is not specified, which means that the function returns nothing.
}

